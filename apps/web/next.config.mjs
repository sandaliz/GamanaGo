/** Minimal Next config placeholder */
// export default {};
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // DO NOT set experimental.appDir to false.
  // Leaving experimental empty keeps App Router ON (default in Next 15).
  experimental: {},
};
export default nextConfig;
